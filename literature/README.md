# literature/ — corpus root (operator-supplied)

Drop PDFs in `pdfs/`. Export text to `text/`. Fan-out writes `ingests/`. Copy `references/corpus-manifest-template.yaml` → `metadata/corpus_manifest.yaml`.

Child `*.skill/` artifacts are generated in **operator corpus sessions** (may live outside this repo).

See `/Users/dubs/Projects/scholia.skill/SKILL.md` § Corpus layout.
