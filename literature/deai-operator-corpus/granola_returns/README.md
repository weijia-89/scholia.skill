# Granola returns — optional archive only

This corpus uses **ChatPRD synthesis** (`chatprd_returns/*_refined_*.md`, `cursor_implement_brief_*.md`) — not Granola S4 re-runs.

If operator saves Granola output anyway, store here for reference only.
